#1.create table with the source data.retrieve data as per output.

CREATE TABLE input (
    resource INT NOT NULL AUTO_INCREMENT,
    Name VARCHAR(25) DEFAULT NULL,
    Profession VARCHAR(25) NOT NULL,
    PRIMARY KEY (resource)
    

)  ENGINE=INNODB AUTO_INCREMENT=5
